name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    RUN mamba install -c conda-forge "python=3.7.*"
    - name: Install dependencies
      run: |
RUN mamba install -c conda-forge rdkit>=2020.09.2
        if grep -q "rdkit" requirements.txt; then
        if grep -q "rdkit" requirements.txt; then

        python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt
    - name: Run tests
      run: |
        python -m unittest discover
    - name: Check CI status
      run: |
        echo "CI status received from 'PolusAI/workflow-inference-compiler'"
        if [ $? -ne 0 ]; then
          echo "Error: Failed to receive CI status"
          exit 1
        fi
