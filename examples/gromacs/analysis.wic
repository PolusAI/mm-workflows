# We previously used &filename to create references to outputs. Now we can
# use *filename to dereference filename and create an explicit edge to the inputs.
steps:
    analysis_realtime.wic:
    analysis_final.wic:

wic:
  graphviz:
    label: Stability Analysis